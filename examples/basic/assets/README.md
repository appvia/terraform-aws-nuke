# Assets

This directory contains the aws-nuke YAML configuration template (`nuke-config.yml.example`) used by the basic example. The template accepts an `account_id` variable for rendering via Terraform's `templatefile()`.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->